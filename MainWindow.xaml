<Window x:Class="WPF_FileExplorer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_FileExplorer" d:DataContext="{d:DesignInstance Type=local:MainViewModel}"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Menu Grid.Row="0" Grid.ColumnSpan="3">
            <MenuItem Header="ファイル">
                <MenuItem Header="開く"></MenuItem>
                <MenuItem Header="閉じる"></MenuItem>
            </MenuItem>
            <MenuItem Header="ヘルプ">
                <MenuItem Header="このアプリについて"></MenuItem>
            </MenuItem>
        </Menu>

        <!--検索ボックス-->
        <Grid Grid.Row="1" Grid.ColumnSpan="3" Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"></ColumnDefinition>
                <ColumnDefinition Width="auto"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="200"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>
            
            <Button Grid.Column="0" Background="Transparent">
                <Image Source="hidariyazirusi.png"></Image>
            </Button>
            <Button Grid.Column="1" Background="Transparent">
                <Image Source="/yazirusi.png"></Image>
            </Button>
            <TextBox Grid.Column="2" Margin="0,0,5,0" Text="{Binding FilePath, UpdateSourceTrigger=PropertyChanged}" KeyDown="onFilePathKeyDown"/>
            <TextBox Grid.Column="3" Margin="0,0,5,0"/>

        </Grid>

        <TreeView Grid.Row="2" Grid.Column="0">
            <TreeViewItem Header="デスクトップ">
                
            </TreeViewItem>
        </TreeView>
        
        <!--ファイル-->
        <WrapPanel Name="FileWrapPanel" Grid.Row="2" Grid.Column="2" Margin="5">
            <!--
            <Button Background="Transparent" Margin="0,0,5,0">
                <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                    <Image Source="folder.png" Width="50" Height="50" />
                    <TextBlock Text="フォルダ1" HorizontalAlignment="Center" />
                </StackPanel>
            </Button>
            -->
        </WrapPanel>

        <GridSplitter Grid.Row="2" Grid.Column="1" Width="5" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
    </Grid>
</Window>
